# Set PATH so it includes pebble dev bin if it exists
if [ "$HOST_NAME" = "iceman" ] && [ -d ~/dev/pebble-dev/arm-cs-tools/bin ] ; then
	PATH=$(_pathedit "$PATH" ~/dev/pebble-dev/arm-cs-tools/bin after)
fi
